# _archive — material histórico

Pasta com artefatos antigos do projeto, mantidos só para referência. Não é executado, não é deployado.

- `scripts/legacy/` — scripts pontuais de migração e testes manuais já aplicados ou substituídos por `scripts/` + `tests/`.
- `docs/roadmap-v2.md` / `docs/roadmap-v3.md` — roadmaps antigos da época em que o produto era o painel de tráfego. Substituídos pela documentação atual em `NEXUS_OS_OVERVIEW.md` e `RUNBOOK.md`.

Pode deletar essa pasta quando quiser; nada no sistema depende dela.
